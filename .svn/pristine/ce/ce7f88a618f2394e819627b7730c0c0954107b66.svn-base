package com.sicap.clientes.vo.cartera;

import java.io.Serializable;
import java.sql.Date;
import java.sql.Timestamp;

public class EventosVO  implements Serializable{
	
	public int numCliente;
	public int numCredito;
	public String tipoEvento;
	public int numDividendo;
	/*public Date fechaIni;
	public Date fechaFin;*/
	public Timestamp fechaIni;
	public Timestamp fechaFin;
	public int  numDias;
	public double monto;
	public int status;
	public String usuario;
	public Double saldo;
	
	public String toString(){
		String respuesta = null;
		respuesta += "numCliete=[" + numCliente + "],";
		respuesta += "numCredito=[" + numCredito + "],";
		respuesta += "tipoEvento=[" + tipoEvento + "],";
		respuesta += "numDividendo=[" + numDividendo + "],";
		respuesta += "numDias=[" + numDias + "],";
		respuesta += "monto=[" + monto + "],";
		respuesta += "status=[" + status + "],";
		respuesta += "usuario=[" + usuario + "],";
		respuesta += "saldo=[" + saldo + "],";
		return respuesta;
	}
	

}
